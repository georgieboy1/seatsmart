# Copy to .env.test or add these as CI secrets when running the full
# authenticated Playwright happy path.
#
# Do not use a real teacher account. Create a disposable Supabase Auth user
# dedicated to E2E testing.

E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=
